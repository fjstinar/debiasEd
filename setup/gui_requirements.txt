# Minimal requirements for DebiasEd GUI
# These are the essential packages needed to run the fairness GUI

numpy>=1.19.0
pandas>=1.3.0
scikit-learn>=1.0.0
matplotlib>=3.3.0
imbalanced-learn>=0.8.0

# Optional but recommended for better performance
scipy>=1.7.0

# Note: tkinter is usually included with Python
# If you get tkinter errors, you may need to install it separately:
# - On Ubuntu/Debian: sudo apt-get install python3-tk
# - On CentOS/RHEL: sudo yum install tkinter
# - On macOS: usually included with Python from python.org 